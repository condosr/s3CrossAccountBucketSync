project="<Project name for cost metrics and tagging>"

createdBy="<User who created resources>"

deployedDate="<Date deployed>"

receivingAccount="<Account number of receiving account>"

externalID="<External id for extra security in trust policy>"

sendingBucketName="<Name of bucket that other receiving account will sync with>"

receivingBucketName="<Name of bucket that sending account will sync its data to>"

kmsKeyArn="<Arn of the kms key the bucket is using (either aws managed or custom)>"

roleName="<Name of the role that will have permissions to both buckets>"